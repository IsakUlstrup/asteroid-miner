export enum ModuleType {
  reactor = "reactor",
  laser = "laser",
  none = "none",
  engine = "engine",
  gravityVortex = "gravityVortex"
}

export enum ItemType {
  equipment
}

export enum ShipType {
  eve,
  retro
}

export enum OreType {
  cyan,
  magenta,
  yellow,
  black
}
