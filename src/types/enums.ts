export enum EquipmentType {
  reactor = "reactor",
  laser = "laser",
  none = "none",
  engine = "engine"
}

export enum ItemType {
  equipment
}

export enum ShipType {
  eve,
  retro
}

export enum OreType {
  cyan,
  magenta,
  yellow,
  black
}
